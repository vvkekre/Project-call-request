import { Account } from "../account";
import { Logger } from "../log";
import * as base from "./base";
import * as api from "./generated/core/api";
import * as models from "./generated/core/models";
export declare class TtsApi extends base.Api<api.TtsApi> {
    constructor(account: Account, log: Logger);
    synthesize(request: models.TtsSynthesisRequestDTO): Promise<Uint8Array>;
    getSpeakers(): Promise<models.TtsSpeakerResponseDTO[]>;
    getSpeakerByName(speakerName: string): Promise<models.TtsSpeakerResponseDTO>;
    deleteSpeakerByName(speakerName: string): Promise<void>;
    updateOrAddSpeakerByName(speakerName: string, voiceFileContent: Uint8Array): Promise<models.TtsSpeakerResponseDTO>;
}
